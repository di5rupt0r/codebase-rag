# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# ChromaDB index (local, machine-specific — not portable across environments)
data/chroma_db/

# Systemd service file — contains machine-specific paths; use the .template instead
systemd/codebase-rag-server.service
.vscode/settings.json
